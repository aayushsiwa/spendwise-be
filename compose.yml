version: "3.8"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: expense-tracker-backend
    ports:
      - "8080:8080"
    volumes:
      - ./records.db:/root/records.db
    env_file:
      - .env
    restart: unless-stopped
